name: transactions_repository
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.3.3

# Add regular dependencies here.
dependencies:
  equatable: ^2.0.5
  fair_n_square: ^0.0.1-alpha.3
  grpc: ^3.2.4

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
